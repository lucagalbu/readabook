import { ExtractSubtitlePipe } from './extract-subtitle.pipe';

describe('ConversionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractSubtitlePipe();
    expect(pipe).toBeTruthy();
  });
});
